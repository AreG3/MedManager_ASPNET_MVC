@model IEnumerable<MedManager.Models.MedTakeModel>

@{
    ViewData["Title"] = "Index";
}

<h1 class="display-4">List of medicines</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Add medication</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dose)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hour)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td style="width:5%">
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td style="width:5%">
                @Html.DisplayFor(modelItem => item.Dose)
            </td>
            <td style="width:5%">
                @Html.DisplayFor(modelItem => item.Hour)
            </td>
            <td style="width:5%">
                @Html.ActionLink("Taken", "Done", new { id = item.MedId }, new { @class = "btn btn-success" }) 
                @Html.ActionLink("Details", "Details", new { id = item.MedId }, new { @class = "btn btn-info" }) 
                @Html.ActionLink("Edit", "Edit", new { id = item.MedId }, new { @class = "btn btn-warning" }) 
                @Html.ActionLink("Delete", "Delete", new { id = item.MedId }, new { @class = "btn btn-danger" })
            </td>
        </tr>
}
    </tbody>
</table>